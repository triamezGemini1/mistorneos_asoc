name: Despliegue de PRUEBAS (BETA)

on:
  push:
    branches:
      - main
      - develop
      - mistorneos_beta
  workflow_dispatch:

jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Subiendo a carpeta BETA (mistorneos_beta)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: public_html/mistorneos_beta/
          security: loose
          dangerous-clean-slate: false
          # Estado nuevo: evita fallos 550 por rutas borradas en el repo (PROD_MISTORNEOS, _LEGACY_RAW).
          state-name: .ftp-deploy-sync-state-beta.json
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/vendor/**
            .env
            **/.env
            **/upload/**
            **/uploads/**
            **/.vscode/**
            **/*.zip
            **/tests/**
            **/_LEGACY_RAW/**
            **/PROD_MISTORNEOS/**
